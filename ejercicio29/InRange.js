let minRange = 50;
let maxRange = 59;

let numero = [52, 45, 53, 59, 60];
const inRange = () => {
  for (number of numero) {
    if (number > 50 && number <= 59) {
      return true;
    } else {
      return false;
    }
  }
};
